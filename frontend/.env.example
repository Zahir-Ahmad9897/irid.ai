# Frontend environment variables (used by Vite at build time)
# In Docker: leave empty — nginx proxies /api to the backend container
# For local dev: uncomment the line below
VITE_API_URL=http://localhost:8000
