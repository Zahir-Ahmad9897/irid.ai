# Comma-separated list of allowed frontend origins for CORS
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# Optional — only needed if the ONNX model must be downloaded from a
# private Hugging Face repo (see src/recognizer.py)
HF_TOKEN=
